# [Project Name] — Claude Code Context

> Generated by the inception agent during project bootstrap.
> Updated by the requirements-analyst agent when the project evolves.
> Claude Code loads this file automatically at the start of every session.
> The `@` imports below pull in the living docs so you always have full project context.

<!-- Living docs — loaded automatically by Claude Code every session -->
@docs/SYSTEM_MEMORY.md
@docs/polaris/polaris.md
@docs/architecture/architecture.md
@docs/security/security.md
@docs/test/test.md
@docs/CHANGELOG.md

<!-- Always-on skills — rules that apply to every task, not just situational ones -->
@skills/brainstorming/SKILL.md
@skills/verification-before-completion/SKILL.md

<!-- Data pipeline guardrails — always active for this project -->
@docs/capability-library/sql-quality.md
@docs/capability-library/sql-spec-testing.md
@docs/capability-library/data-quality-checks.md
@docs/capability-library/terraform-security.md

## Always Active Rules

These rules apply to every task in every session. They do not require a skill to be invoked.

- **IDF R1:** Intent describes client outcomes, not task lists — "what will be true for the client" not "what to build"
- **IDF R2:** Ambiguity stops execution — ask before building; do not interpret ambiguous intent
- **IDF R3:** Every user-visible change ships behind a feature flag — no exceptions
- Before modifying code: check `docs/idf/feature-governance.md` for active flags on the same component
- Before claiming any work is done: run the verification-before-completion gate (already loaded above)

### Data Pipeline Guardrails (always active)

- **SQL quality:** Before completing any SQL file, data pipeline step, or table orchestration — run the lint checklist and performance review in `docs/capability-library/sql-quality.md`. No `SELECT *`, filters must precede joins, linter must pass clean.
- **SQL spec testing:** Any model containing a window function, deduplication logic, UDF, conditional aggregation, date arithmetic, pivot, or multi-branch CASE must have a SPEC file at `specs/<model>/<behaviour>.spec.sql`. Rules in `docs/capability-library/sql-spec-testing.md`. A model with complex logic and no SPEC file is not done.
- **Data quality checks:** Every table produced by the pipeline must have quality tests (not_null, unique, accepted_values, referential integrity, row count assertion) as defined in `docs/capability-library/data-quality-checks.md`. A table without tests is not done.
- **Terraform security:** Before completing any `.tf` file — run the security checklist in `docs/capability-library/terraform-security.md`. No secrets in code, no public storage, no over-privileged IAM, automated scanner must pass.
- **PR validation:** Every PR to main runs three automated checks — SQL formatting (SQLFluff), schema breaker (DAG impact), and AI review (GitHub Copilot). All three must pass before merge. Rules and setup in `docs/capability-library/pr-validation.md`.

## Project Identity

| Field | Value |
|---|---|
| **Project** | [Project Name] |
| **Client** | [Client Name] |
| **Team** | [Team Name] |
| **Repository** | [Repository URL] |

## What We Are Building

[2-3 sentence summary of the project, populated by the inception agent from the validated Intent Document. This gives Claude immediate context on the problem being solved and why.]

## Tech Stack

[Populated by inception agent — mirrors docs/architecture/architecture.md tech stack table]

## Key Constraints

[Populated by inception agent — e.g. cloud provider mandated by client, GDPR compliance required, target delivery YYYY-MM-DD, performance targets, platform mandates]

## Repository Structure

```
docs/
├── SYSTEM_MEMORY.md             # Running project context and cycle log           (@-imported above)
├── CHANGELOG.md                 # Significant changes narrative                   (@-imported above)
├── TEMPLATE_GUIDE.md            # Human guide: what to customise, ownership model
├── polaris/polaris.md           # Client vision, deliverables, acceptance criteria (@-imported above)
├── architecture/architecture.md # Tech stack, components, design decisions         (@-imported above)
├── security/security.md         # Security requirements and controls               (@-imported above)
├── test/test.md                 # Testing strategy and coverage targets            (@-imported above)
├── idf/                         # IDF delivery artifacts (intent-log, gate-reports, drift-register…)
├── capability-library/          # Domain-specific instruction sets for Builder agents
├── designs/                     # Design specs from brainstorming (YYYY-MM-DD-<topic>-design.md)
└── plans/                       # Implementation plans from writing-plans (YYYY-MM-DD-<feature>.md)
agents/                          # AI agents — see agents/README.md
skills/                          # AI skills — invoke when the situation matches
```

## Agents

See [agents/README.md](agents/README.md) for full descriptions and invocation instructions.

| Agent | When to Invoke |
|---|---|
| `inception` | Setting up this project for the first time after cloning |
| `orchestrator` | Start of every delivery cycle — decomposes PO intent into a task list; also run at cycle close to update SYSTEM_MEMORY.md |
| `guardian` | After Builder agents complete work — produces ≤3 paragraph gate report for PO |
| `onboarding-guide` | A new team member is joining the project |
| `code-reviewer` | After completing a feature branch, before raising a PR |
| `requirements-analyst` | New client input or change request received |
| `incident-responder` | Production incident — live feature causing errors or data risk |
| `release-manager` | After guardian PASS — coordinate flag activation and cycle close |
| `drift-analyst` | Every 10–20 cycles, or when gate first-pass rate drops below 60% |
| `dependency-broker` | Cross-team dependency detected during orchestrator decomposition |
| `project-adoption` | Adopting this template structure into an existing project |

## Skills

Invoke skills when the corresponding situation arises during a session:

| Skill | Trigger | Always-on |
|---|---|---|
| `brainstorming` | About to create a feature, component, or any new behaviour — design before code | ✅ |
| `verification-before-completion` | About to claim anything is done, fixed, or passing | ✅ |
| `writing-intent` | PO is writing a cycle intent statement — enforces IDF R1 (outcomes, not tasks) | |
| `writing-plans` | Have an approved design, need a step-by-step implementation plan | |
| `subagent-driven-development` | Executing a plan with independent tasks in this session | |
| `dispatching-parallel-agents` | 2+ independent problems that can be investigated simultaneously | |
| `requesting-code-review` | Feature complete, before merging — triggers code-reviewer agent | |
| `finishing-a-development-branch` | Implementation done, tests pass — guides merge/PR/discard decision | |
| `using-git-worktrees` | Starting feature work that needs branch isolation | |
| `hotfix` | Production incident — rollback via flag or expedited hotfix branch | |
| `debugging` | Test failing or unexpected behaviour — reproduce-first, hypothesis-driven | |
| `database-migrations` | Any schema change — expand/contract pattern, sequencing in cycles | |
| `context-reset` | Every 5–10 cycles, or gate first-pass rate is declining — prunes SYSTEM_MEMORY and capability-library | |
| `capability-harvest` | Guardian or Orchestrator detects a repeated pattern — encode it into capability-library | |
| `project-inception` | Cloned the template, project not yet bootstrapped | |
| `onboarding` | New team member needs to get up to speed | |
| `updating-docs` | A doc needs to reflect new decisions or changes | |
| `creating-snapshots` | Sprint end, milestone reached, or baseline approved | |

## Versioning Conventions

- **Living docs** — single file per area (e.g. `docs/polaris/polaris.md`), Git is the full change history
- **Frontmatter** — every doc has `version`, `status` (draft / approved / superseded), `last-updated`, `owner`
- **Snapshots** — copy to `docs/<area>/archive/YYYY-MM-DD.md` when a baseline is approved
- **Git tags** — `git tag <milestone>-approved` for each approved baseline
- **CHANGELOG.md** — human narrative entry for every significant change

## Working Agreements

- All code changes go through pull requests — no direct commits to main
- Commit messages follow conventional commits: `type(scope): description`
- Tests must pass in CI before merging (see docs/test/test.md)
- When an architectural, security, or requirements decision changes, update the relevant doc and bump its `version`
