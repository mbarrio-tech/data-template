name: data_pipeline_example
version: '1.0.0'
config-version: 2

profile: data_pipeline_example

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  data_pipeline_example:
    staging:
      +materialized: view
    marts:
      +materialized: table
